version: "3.7"

services:
  db:
    image: mariadb:10
    containername: td-wp-db
    restart: always
    environment:
      MYSQL_DATABASE: "${MYSQL_DATABASE}"
      MYSQL_USER: "${MYSQL_USER}"
      MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
    volumes:
      - db:/var/lib/mysql

  wordpress:
    image: wordpress:5.5.1-fpm-alpine
    containername: td-wp-site
    restart: always
    volumes:
      - wordpress:/var/www/html
    depends_on:
      - db
    environment:
      WORDPRESS_DB_HOST: "${WORDPRESS_DB_HOST}"
      WORDPRESS_DB_NAME: "${WORDPRESS_DB_NAME}"
      WORDPRESS_DB_USER: "${WORDPRESS_DB_USER}"
      WORDPRESS_DB_PASSWORD: "${WORDPRESS_DB_PASSWORD}"

  nginx:
    image: nginx:alpine
    containername: td-wp-nginx
    restart: always
    depends_on:
      - wordpress
    ports:
      - 8080:80
    volumes:
      - ./conf.d:/etc/nginx/conf.d
      - wordpress:/var/www/html

  phpmyadmin:
    image: phpmyadmin:5.0.2-fpm-alpine
    containername: td-wp-phpmyadmin
    restart: always
    depends_on: db
    ports:
      - 8080:80
    environment:
      PMA_ARBITRARY: 1
      PMA_HOST: db
      MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
    

volumes:
  db:
  wordpress:
      
